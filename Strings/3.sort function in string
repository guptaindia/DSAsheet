#include<bits/stdc++.h>
using namespace std;
void sort(string &s1)
{
	sort(s1.begin(),s1.end());
	int n = s1.length();
	for(int i=0;i<n;i++)
	{
		cout<<s1[i];
	}
}
int main()
{
	string s1 = "mnbvc";
	sort(s1);
}
